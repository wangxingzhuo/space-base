.record-trade {
  border-radius: 4px;
  padding: 20px;
  width: 280px;
  color: #fff;
  background: var(--primary-bg-color);

  .record-trade__title {
    margin-bottom: 12px;
  }

  .record-form {
    display: flex;
    flex-direction: column;
  }
}

:global(.some-input), :global(.some-selector) {
  margin: 8px 0;
  color: #fff;
}
